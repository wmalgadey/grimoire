// Composition root for the standalone eval command (ADR-011). Subcommand wiring
// arrives with the CLI shell task; this stub only reserves the entry point so the
// ADR-011 structural rules (C6–C8) have an assembly to guard from Phase 0 on.
return 0;
